# Copy this file to .env and fill values
STREAM_KEY=your_stream_api_key
STREAM_SECRET=your_stream_api_secret
PORT=8080
# Comma-separated list of allowed web origins (optional)
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000

# Supabase configuration (optional but recommended)
# SUPABASE_URL=https://your-project-ref.supabase.co
# SUPABASE_EXPECTED_AUD=authenticated

# Optional: set to require X-Token-Server-Secret header on all requests
# TOKEN_SERVER_SECRET=your-random-secret-here